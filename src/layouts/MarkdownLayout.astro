---
import BaseLayout from "./BaseLayout.astro"

const { frontmatter: frontmatterOrEmpty } = Astro.props


const title = 
  frontmatterOrEmpty == undefined || frontmatterOrEmpty.title === ""
    ? "Placeholder"
    : frontmatterOrEmpty.title
---


<BaseLayout title={title}>
  <div class="p-12">
    <h1>{title}</h1>
    <slot />
  </div>
</BaseLayout>
